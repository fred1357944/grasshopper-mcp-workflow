# Multi-AI Optimizer Plugin

> Claude + Gemini 協作優化 Plugin，支援迭代設計優化和多變體探索

## 概述

本 Plugin 實現雙 AI 協作模式，讓 Claude 和 Gemini 交替生成和評論設計方案，達到更高品質的設計輸出。

## 核心功能

### 兩種優化模式

| 模式 | 說明 | 適用場景 |
|------|------|----------|
| **Option A: 迭代優化** | 單一設計，多次精煉 | 有明確目標，需要逐步改進 |
| **Option B: 多變體探索** | 多個設計，並行評估 | 不確定方向，想比較可能性 |

### 智能確認機制

在以下時機自動暫停等待用戶確認：
- 🔴 **關鍵決策** - 架構選擇、技術方向變更
- 🟢 **收斂達成** - 兩 AI 意見趨於一致
- 🟡 **最大次數** - 達到指定迭代上限

## 快速開始

### 1. 探索設計需求

```
/thinking "我需要設計一個參數化桌子"
```

### 2. 啟動優化

```
# 自動選擇模式
/optimize "優化桌子的組件連接"

# 指定迭代模式，最多 5 次
/optimize "改進設計" iterative 5

# 指定多變體模式，生成 3 個變體
/optimize "探索不同設計" variants 3
```

### 3. 回顧結果

```
/review
```

## 命令列表

| 命令 | 說明 |
|------|------|
| `/optimize [topic] [mode] [iterations]` | 啟動優化流程 |
| `/thinking [topic]` | 進入探索模式 |
| `/review [session_id]` | 回顧優化結果 |

## Skills

| Skill | 說明 |
|-------|------|
| `design-optimizer` | 設計優化核心 Skill |
| `thinking-partner` | 協作探索 Skill |

## 與 Grasshopper 工作流整合

```
Step 1: 需求澄清
    └── /thinking 探索設計需求

Step 2: 幾何分解 (part_info.mmd)
    └── /optimize 優化結構

Step 3: 組件連接 (component_info.mmd)
    └── /optimize 優化連接

Step 4-5: GUID 解析 + 執行
    └── 使用現有 MCP 工具

Step 6: 回顧
    └── /review 總結優化過程
```

## 配置

### plugin.json 設定

```json
{
  "settings": {
    "default_max_iterations": 5,
    "convergence_threshold": 0.85,
    "auto_gemini_timeout": 60,
    "confirmation_triggers": [
      "critical_decision",
      "convergence_reached",
      "max_iterations_reached"
    ]
  }
}
```

### 調整設定

在對話中說：
- "設定最大迭代次數為 10"
- "調整收斂閾值到 0.9"
- "關閉自動確認"

## 狀態持久化

所有優化過程保存在 `GH_WIP/optimization_session/`：

```
GH_WIP/optimization_session/
├── session_state.json      # 當前狀態
├── proposals/              # 所有提案
│   ├── 001_claude.md
│   ├── 002_gemini.md
│   └── ...
├── decisions.json          # 決策記錄
├── optimization_log.md     # 完整日誌
└── review_report.md        # 回顧報告
```

## Gemini 整合

本 Plugin 通過終端機調用 Gemini CLI：

```bash
# 基本調用
gemini "請評論這個設計方案..."

# 結構化輸出
gemini "分析以下內容，以 JSON 格式回應..."
```

確保已安裝 Gemini CLI：
```bash
which gemini  # 應該顯示路徑
```

## 依賴

- Gemini CLI（已安裝在系統中）
- Python 3.8+
- grasshopper_mcp.langgraph 模組

## 版本

- **Plugin 版本**: 0.1.0
- **相容 Claude Code 版本**: 1.0+

## 相關資源

- [Claudesidian 設計參考](https://github.com/heyitsnoah/claudesidian)
- [LangGraph 文檔](https://langchain-ai.github.io/langgraph/)
- [Grasshopper MCP Workflow](../../../README.md)
