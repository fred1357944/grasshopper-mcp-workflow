# 螺旋樓梯開發完整記錄

> 從零到成功部署的完整過程，包含所有踩坑和解決方案
> 日期：2026-01-23

---

## 專案概覽

### 目標
使用 GH_MCP (Grasshopper Model Context Protocol) 自動化建立一個參數化螺旋樓梯，包含：
- 踏板 (Steps)
- 中心柱 (Center Pole)
- 扶手 (Handrail)

### 最終成果
- **35 個組件**成功創建
- **48 條連接**成功建立
- 完整的參數化設計，可調整階數、高度、半徑等

### 關鍵檔案位置
```
patterns/
├── index.json                          # Pattern Library 索引
└── spiral-staircase-v1/
    ├── pattern.json                    # 完整 Pattern 資料 + trusted GUIDs
    └── flowchart.mmd                   # Mermaid 組件連接圖

scripts/
└── build_spiral_staircase.py           # 部署腳本

config/
└── trusted_guids.json                  # 驗證過的組件 GUID

docs/
├── GH_MCP_API_GUIDE.md                 # API 使用指南
└── GH_MCP_DEBUG_KNOWLEDGE.md           # 除錯知識庫
```

---

## 開發時間線

### Phase 1: 初始嘗試與失敗

**問題 1: Panel vs Number Slider**

```
症狀：連線顯示虛線，Division 組件報錯
      "Input parameter A failed to collect data"

原因：Panel 輸出的是「文字」，數學組件需要「數值」

錯誤做法：
  add_component(type="Panel", ...)
  set_component_value(id=panel_id, value="360")

正確做法：
  add_component(type="Number Slider", ...)
  set_slider_properties(id=slider_id, min=0, max=720, value=360)
```

**問題 2: Slider 數值被 Clamp 到 1.000**

```
症狀：設定 value=360，但顯示 1.000

原因：Slider 預設範圍是 0-1，先設 value 會被 clamp

錯誤順序：
  set_slider_properties(id=id, value=360)      # 被 clamp 到 1
  set_slider_properties(id=id, min=0, max=720) # 太晚了

正確順序：
  set_slider_properties(id=id, min=0, max=720) # 先設範圍
  set_slider_properties(id=id, value=360)      # 再設數值
```

### Phase 2: 組件類型衝突

**問題 3: Rotate 組件類型錯誤**

```
症狀：Rotate 顯示 Vector/Axis/Angle 輸入，而非 Geometry/Angle/Plane

原因：MCP fuzzy search 返回 VectorComponents/Rotate（第三方插件）
      而不是 XformComponents/Rotate（原生幾何旋轉）

錯誤做法：
  add_component(type="Rotate", ...)  # MCP 自動選擇，可能選錯

正確做法：
  ROTATE_GUID = "19c70daf-600f-4697-ace2-567f6702144d"
  add_component(type="Rotate", guid=ROTATE_GUID, ...)
```

**問題 4: OBSOLETE 組件版本**

```
症狀：Rotate 組件顯示 "OLD" 標籤

原因：GUID 5944e8e2-9fb9-4f8b-bdd4-8b18f1955360 是舊版（OBSOLETE）

解決：查詢非 OBSOLETE 的 GUID
      新版 GUID: 19c70daf-600f-4697-ace2-567f6702144d
```

### Phase 3: 成功部署

修正所有問題後，螺旋樓梯成功部署：

```
✓ 組件創建: 35/35
✓ 連接建立: 48/48
```

---

## 技術細節

### 核心演算法：極座標轉直角座標

```
螺旋樓梯的數學邏輯：

1. 生成序列: Series(0, 1, Steps) → [0, 1, 2, ..., N-1]

2. 計算角度:
   - TotalAngle = 360 × Turns
   - AnglePerStep = TotalAngle / Steps
   - StepAngles = Series × AnglePerStep

3. 極座標轉直角座標:
   - Radians = StepAngles × (π/180)
   - X = MidRadius × Cos(Radians)
   - Y = MidRadius × Sin(Radians)
   - Z = StepHeights

4. 幾何生成:
   - 踏板: Center Box → Rotate（對齊角度）
   - 中心柱: Cylinder
   - 扶手: Interpolate → Pipe
```

### 組件參數名對照表

| 組件類型 | 輸入參數 | 輸出參數 |
|---------|---------|---------|
| Number Slider | - | N |
| Series | S, N, C | S |
| Division | A, B | R |
| Multiplication | A, B | R |
| Addition | A, B | R |
| Subtraction | A, B | R |
| Radians | D | R |
| Sine | x | y |
| Cosine | x | y |
| Construct Point | X, Y, Z | Pt |
| Center Box | B, X, Y, Z | Box |
| Rotate | G, A, P | G |
| Cylinder | B, R, L | Cyl |
| Interpolate | V | C |
| Pipe | C, R | Pipe |

### Trusted GUIDs（經驗證）

```json
{
  "Number Slider": "57da07bd-ecab-415d-9d86-af36d7073abc",
  "Series": "651c4fa5-dff4-4be6-ba31-6dc267d3ab47",
  "Addition": "d65c653b-7a82-4666-bd4b-a71f3bd841a6",
  "Subtraction": "0ff0bb57-8207-48a0-a732-6fd4d4931193",
  "Multiplication": "2ce3f211-76d2-49e5-9f2f-7e75deed1c0e",
  "Division": "b16a2ec0-f873-4ef7-8e0c-a068e7571cb4",
  "Radians": "a337f645-460f-4f2a-892e-5f7f1ebb51a4",
  "Sine": "96061e03-a68a-4d5d-a02e-f215d27f2d83",
  "Cosine": "1ac463e0-156f-4024-988b-b61bc57965f8",
  "Construct Point": "3581f42a-9592-4549-bd6b-1c0fc39d067b",
  "Center Box": "d1296e28-f64c-4c2a-9a9e-49e7839460de",
  "Cylinder": "4edaf2ed-7b3a-42ed-bce0-3119ed106792",
  "Rotate": "19c70daf-600f-4697-ace2-567f6702144d",
  "Interpolate": "c731696a-ea52-4c47-be86-e64bc80bde08",
  "Pipe": "1ee25749-2e2d-4fc6-9209-0ea0515081f9"
}
```

---

## 截圖記錄

### 成功部署的 Grasshopper 畫布

截圖位置：
- 原始：`/Users/laihongyi/Library/Application Support/CleanShot/media/media_Up4Hi81ugP/CS-20260123@193522@2x.jpg`
- 專案備份：`docs/screenshots/spiral_staircase_gh_canvas.jpg`

**畫布結構分析（從左到右）**：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  左側區域 (參數 Sliders)                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│  Count  ○12.000  ──┬──→ Panel ──→ Series                                    │
│                    │                                                         │
│  A      ○300.000 ──┼──→ A×B (Multiplication)                                │
│                    │     ↓                                                   │
│  A      ○120.000 ──┼──→ A×B ──→ A×B ──→ ...                                 │
│                    │                                                         │
│  B      ○25.000  ──┼──→ Panel ──→ ...                                       │
│                    │    Panel                                                │
│  A      ○1.000   ──┤                                                         │
│                    │                                                         │
│  Z      ○5.000   ──┤                                                         │
│                    │                                                         │
│  B      ○90.000  ──┤                                                         │
│                    │                                                         │
│  Radius ○3.000   ──┴──→ Panel×5 (除錯用)                                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  中間區域 (計算邏輯)                                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│  Series → A×B → A×B → Rad → Sin/Cos → A×B → ...                             │
│                              ↓                                               │
│                         B ○2.000 (常數)                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  右側區域 (幾何生成)                                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│  ... → Pt (Construct Point) → Box → Rotate                                  │
│                               ↓                                              │
│  ... → Pt ──────────────────→ Cyl (Cylinder)                                │
│                               ↓                                              │
│  ... → Pt → A×B ────────────→ Pt → IntCrv → Pipe                            │
└─────────────────────────────────────────────────────────────────────────────┘
```

**參數數值對照**：

| Slider | 數值 | 用途 |
|--------|------|------|
| Count | 12.000 | 階梯數量 |
| A | 300.000 | 總高度 (cm) |
| A | 120.000 | 外半徑 (cm) |
| B | 25.000 | 內半徑 (cm) |
| A | 1.000 | 旋轉圈數 |
| Z | 5.000 | 踏板厚度 (cm) |
| B | 90.000 | 扶手高度偏移 (cm) |
| Radius | 3.000 | 扶手管徑 (cm) |
| B | 2.000 | 常數 (用於除法計算中點) |

**黃色 Panel 用途**：
- 左下角 5 個 Panel：顯示除錯資訊（中間計算結果）
- 中間 2 個 Panel：顯示角度和高度序列

**連接密度觀察**：
- 連線密集但有組織
- 從左到右的資料流清晰
- 多個 A×B (Multiplication) 組件處理不同計算

### 連接詳細說明

從截圖可見的資料流：

```
1. 階梯序列生成
   Count (12) → Series.C → [0,1,2,...,11]

2. 角度計算
   A (300) × A (1) → 總旋轉角度
   總旋轉角度 / Count → 每階角度
   Series × 每階角度 → 各階角度序列

3. 高度計算
   A (300) / Count → 每階高度
   Series × 每階高度 → 各階高度序列

4. 極座標轉換
   角度序列 → Radians → Sin/Cos
   (A (120) + B (25)) / B (2) → 中點半徑
   中點半徑 × Cos → X 座標
   中點半徑 × Sin → Y 座標

5. 踏板生成
   Pt(X, Y, Z) → Box(寬, 深, Z(5)) → Rotate(角度)

6. 中心柱
   Pt(0, 0, 0) + B (25) → Cyl(半徑, A(300))

7. 扶手
   各階 Z + B (90) → 扶手高度
   Pt(扶手X, 扶手Y, 扶手Z) → IntCrv → Pipe(Radius(3))
```

---

## Pattern Library 整合

### 為什麼建立 Pattern Library？

1. **避免重複踩坑**：trusted GUIDs 已驗證，直接使用
2. **加速開發**：下次設計螺旋樓梯，搜尋即可重用
3. **持續學習**：用戶回饋會更新 elegance_score

### Pattern 資料結構

```json
// patterns/spiral-staircase-v1/pattern.json
{
  "pattern_id": "spiral-staircase-v1",
  "name": "螺旋樓梯 (Spiral Staircase)",
  "description": "使用 Sin/Cos 極座標生成螺旋階梯，包含中心柱和扶手",

  "parameters": [
    {"name": "Steps", "default": 12, "min": 6, "max": 30},
    {"name": "TotalH", "default": 300, "min": 150, "max": 500},
    {"name": "OuterR", "default": 120, "min": 80, "max": 200},
    {"name": "InnerR", "default": 25, "min": 15, "max": 50},
    {"name": "Rotation", "default": 1.0, "min": 0.5, "max": 3.0},
    {"name": "StepT", "default": 5, "min": 3, "max": 10},
    {"name": "HandrailH", "default": 90, "min": 60, "max": 120},
    {"name": "PipeR", "default": 3, "min": 2, "max": 6}
  ],

  "trusted_guids": {
    "Series": "651c4fa5-dff4-4be6-ba31-6dc267d3ab47",
    "Multiplication": "2ce3f211-76d2-49e5-9f2f-7e75deed1c0e",
    "Rotate": "19c70daf-600f-4697-ace2-567f6702144d",
    // ... 完整列表見 pattern.json
  },

  "metadata": {
    "elegance_score": 4.5,
    "usage_count": 1,
    "verified": true
  }
}
```

### 搜尋測試

```python
from grasshopper_mcp.pattern_library import search_patterns

results = search_patterns("螺旋樓梯", top_k=3)
# 輸出:
# 1. 螺旋樓梯 (Spiral Staircase)
#    分數: 49.0 | 優雅: ⭐⭐⭐⭐ (4.5/5)
```

---

## Skill 工作流程設計

### 七階段狀態機

```
Phase 0: Pattern Library 搜尋
    ↓ 找到 Pattern → 用戶選擇使用/從頭設計
Phase 1: 需求釐清 (CLARIFY)
    ↓ 用戶確認規格
Phase 2: 幾何分解 (DECOMPOSE)
    ↓ 用戶確認 erDiagram
Phase 3: 組件規劃 (PLAN)
    ↓ 用戶確認 flowchart
Phase 4: GUID 查詢 (QUERY)
    ↓ 自動進入
Phase 5: 執行部署 (EXECUTE)
    ↓ 部署完成
Phase 6: 回饋收集與歸檔 (ARCHIVE)
```

### 關鍵設計原則

1. **先展示再寫入**：每個階段先在對話中展示內容，等用戶確認才寫檔
2. **不要一次跑到底**：每個階段都需要用戶明確確認
3. **收集回饋**：部署完成後收集 👍/👎/✏️ 回饋，更新 elegance_score

---

## 經驗總結

### MCP API 黃金法則

```
1. Slider 設定順序：先 min/max，再 value
2. 常數值用 Number Slider，不用 Panel
3. 有衝突風險的組件，指定 trusted GUID
4. 參數名用 NickName（短名），不用 Name（全名）
```

### 除錯 SOP

```
1. 連線虛線 → 檢查源組件類型（Panel 改 Slider）
2. 數值錯誤 → 檢查 Slider 設定順序
3. OLD 標籤 → 查詢非 OBSOLETE 的 GUID
4. 組件類型錯誤 → 使用 trusted GUID
```

### 文檔優先級

對話壓縮後，Claude 應優先閱讀：
1. `docs/GH_MCP_API_GUIDE.md` — API 正確用法
2. `docs/GH_MCP_DEBUG_KNOWLEDGE.md` — 踩坑記錄
3. `patterns/spiral-staircase-v1/pattern.json` — trusted GUIDs

---

## 下一步開發方向

1. **測試完整 Skill 流程**：驗證 Phase 0-6 狀態機正確運作
2. **增加更多 Pattern**：參數化桌子、椅子、展示架
3. **LangGraph 整合**：把狀態機規則抽象成 conditional edge
4. **A/B 比對機制**：讓用戶比較新設計與已有 Pattern

---

*此文檔記錄了 GH_MCP 螺旋樓梯專案的完整開發過程，作為未來參考和教學素材。*
